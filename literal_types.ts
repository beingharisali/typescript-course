let directions: "Top" | "Right" | "Bottom" | "Left";
directions = "Top";
console.log(directions);
function setColors(colors: "red" | "green" | "blue" | "white") {
  return colors;
}

console.log(setColors("blue"));
